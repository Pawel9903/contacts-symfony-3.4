
<nav class="navbar navbar-expand navbar-dark bg-dark rounded">
    <div class="container">
        <a href="#" class="navbar-brand border-right pr-2">{{ 'logo'|trans }}</a>
                <div class="navbar-nav mr-auto">

                    <a href="{{ url('contacts_index') }}" class="nav-link nav-item">{{ 'home'|trans }}</a>
                    <a href="{{ url('contacts_contacts') }}" class="nav-link nav-item">{{ 'other contacts'|trans }}</a>
                    <a href="{{ url('my_contacts_contacts') }}" class="nav-link nav-item">{{ 'my contacts'|trans }}</a>
                    <a href="{{ url('my_contacts_add') }}" class="nav-link nav-item">{{ 'add contact'|trans }}</a>
                </div>
            <form action="#" class="form-inline">
                <input type="text" class="form-control">
                <button class="btn btn-light mx-2">{{ 'search'|trans }}</button>
            </form>
        <span class="navbar-text mx-4">23.02.2018</span>
    </div>
</nav>